#include <bits/stdc++.h>

using namespace std;

int main(void){
    int t;
    cin >> t;
    while (t--){
        int y, x;
        cin >> y >> x;

        if (x >= y){
            int diagonal = diagonal(x - 1) + 2*(x-1);
        } else {

        }
    }
    
    
    return 0;
}